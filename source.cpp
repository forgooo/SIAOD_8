#include "First.h"
#include "Second.h"

int main() {
//    string data;
//    srand(time(NULL));
//    for (int SIZE = 100; SIZE != 10000000; SIZE *= 10) {
//        cout << "SIZE = " << SIZE << " TIME = ";
//        for (int i = 0; i < SIZE; i++) data += random_string(8) + ", ";
//        for(int i = 0; i < 10; i++){
//            while(true) {
//                int r = rand() % SIZE;
//                if(data.find(", ", r, r+4) == string::npos){
//                    data[r] = 'a';data[r+1] = 'u';data[r+2] = 't';data[r+3] = 'o';
//                    break;
//                }
//            }
//        }
//        auto start = std::chrono::high_resolution_clock::now();
//        getWordsWithSubstring(data, "auto");
//        auto end = std::chrono::high_resolution_clock::now();
//        auto duration = std::chrono::duration_cast<std::chrono::nanoseconds>(end - start);
//        cout << duration.count() << "ns\n";
//    }



//    for (int SIZE = 100; SIZE != 10000000; SIZE *= 10) {
//        cout << "SIZE = " << SIZE << " ";
//        std::string str = generateRandomString(SIZE);
//        auto start = std::chrono::high_resolution_clock::now();
//        std::string longestPalindrome = findLongestPalindrome(str);
//        std::cout << "Longest palindrome substring: " << longestPalindrome << std::endl;
//        auto end = std::chrono::high_resolution_clock::now();
//        auto duration = std::chrono::duration_cast<std::chrono::nanoseconds>(end - start);
//        cout << duration.count() << "ns\n";
//    }



}